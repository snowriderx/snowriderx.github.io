[pytest]
testpaths = tests
python_files = test_*.py
addopts =
    --strict-markers
    --tb=short
    -v
markers =
    unit: pure function tests, no DB, no server
    integration: tests that hit the real PostgreSQL DB via Flask test client
    slow: slow tests
